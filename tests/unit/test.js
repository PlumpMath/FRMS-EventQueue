var httputil = require('nodeunit').utils.httputil;

//	Sanity check to verify nodeunit working
exports.testRouteToListingsController = function(test) {
        test.ok(true, 'everything is ok');
        test.done();
};

